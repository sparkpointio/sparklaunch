import { createReducer } from "@reduxjs/toolkit";
import { TransactionState } from "state/type";

const initialState: TransactionState = { };

// export default createReducer(initialState, builder => {
//     builder
//         .addCase()
// })